[project]
name = "linkedin-mcp-server"
version = "0.1.0"
description = "MCP server exposing LinkedIn actions as tools"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.6.0",
    "linkedin-api>=2.2.0",
    "python-dotenv>=1.0.0",
    "playwright>=1.40.0",
]
